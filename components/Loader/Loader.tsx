import React from 'react'
import "./Loader.css"
import config from "@/app/config";

type Props = {}

const Loader = (props: Props) => {
  return (
    <div>Loader</div>
  )
}

export default Loader